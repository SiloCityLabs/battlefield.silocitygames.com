export type GeneratorItem = {
  name: string;
  type: string;
  game: string;
};

export type Weapon = {
  name: string;
  type: string;
  game: string;
  no_attach?: boolean;
  no_attach_info?: boolean;
};
